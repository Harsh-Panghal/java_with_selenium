pipeline {
    agent any
 
    tools {
        maven 'Maven_3'
    }
 
    stages {
 
        stage('Checkout') {
            steps {
                git branch: 'master',
                    url: 'https://github.com/Harsh-Panghal/java_with_selenium'
            }
        }
 
        stage('Build') {
            steps {
                dir('OpenCart_Automation') {
                    bat 'mvn clean'
                }
            }
        }
 
        stage('Compile') {
            steps {
                dir('OpenCart_Automation') {
                    bat 'mvn compile'
                }
            }
        }
 
        stage('Test') {
            steps {
                dir('OpenCart_Automation') {
                    bat 'mvn test'
                }
            }
        }
 
        stage('Package') {
            steps {
                dir('OpenCart_Automation') {
                    bat 'mvn package'
                }
            }
        }
    }
 
    post {
        success {
            echo 'Build Successful'
        }
 
        failure {
            echo 'Build Failed'
        }
    }
}